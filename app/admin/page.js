import { AdminConsole } from "@/components/admin-console";
export const metadata = { title: "Trust operations", robots: { index: false, follow: false } };
export default function AdminPage() { return <AdminConsole />; }
