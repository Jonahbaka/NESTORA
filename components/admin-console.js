"use client";
import { useState } from "react";
import { AlertTriangle, BadgeCheck, BarChart3, Check, ChevronRight, Clock3, FileSearch, Flag, LayoutDashboard, MessageSquareWarning, Search, ShieldCheck, UserCheck, UsersRound, X } from "lucide-react";

const reports = [
  { id: "RPT-1048", type: "Listing accuracy", subject: "Service charge differs from listing", risk: "High", age: "18 min", owner: "Unassigned" },
  { id: "RPT-1047", type: "Message safety", subject: "Requested payment outside Nestora", risk: "High", age: "32 min", owner: "K. James" },
  { id: "RPT-1043", type: "Community conduct", subject: "Harassing replies in neighbourhood group", risk: "Medium", age: "2 hr", owner: "M. Opara" },
  { id: "RPT-1039", type: "Duplicate listing", subject: "Likely copied images and address", risk: "Low", age: "4 hr", owner: "Unassigned" },
];

export function AdminConsole() { const [queue, setQueue] = useState(reports); const [selected, setSelected] = useState(reports[0]); const [section, setSection] = useState("Overview"); const resolve = (id) => { setQueue((items) => items.filter((item) => item.id !== id)); setSelected(null); }; return <div className="admin-shell"><aside className="admin-sidebar"><div><ShieldCheck size={23} /><span><strong>Nestora Trust</strong><small>Operations console</small></span></div><nav>{[["Overview",LayoutDashboard],["Reports",Flag],["Verification",UserCheck],["Content review",FileSearch],["Community safety",UsersRound],["Audit log",BarChart3]].map(([item,Icon]) => <button type="button" className={section === item ? "active" : ""} onClick={() => setSection(item)} key={item}><Icon size={17} />{item}{item === "Reports" ? <b>{queue.length}</b> : null}</button>)}</nav><p><ShieldCheck size={14} /> Administrative actions are recorded and reviewed.</p></aside><main className="admin-main"><header><div><p className="eyebrow">Restricted workspace</p><h1>{section}</h1></div><label><Search size={16} /><input placeholder="Search case, account or listing" /></label><span>TO</span></header><div className="admin-content"><section className="admin-metrics"><article><span><Flag size={18} /></span><div><small>Open reports</small><strong>{queue.length}</strong></div><em>2 high priority</em></article><article><span><Clock3 size={18} /></span><div><small>Median first response</small><strong>21 min</strong></div><em>Within target</em></article><article><span><UserCheck size={18} /></span><div><small>Verification queue</small><strong>18</strong></div><em>6 due today</em></article><article><span><MessageSquareWarning size={18} /></span><div><small>Proactive signals</small><strong>7</strong></div><em>Automated review</em></article></section><div className="admin-grid"><section className="review-queue"><div className="panel-heading"><div><h2>Priority review queue</h2><p>Reports ordered by potential harm and evidence freshness.</p></div><button type="button">All reports <ChevronRight size={15} /></button></div><header><span>Case</span><span>Type and subject</span><span>Risk</span><span>Age</span><span>Owner</span></header>{queue.map((report) => <button type="button" key={report.id} className={selected?.id === report.id ? "active" : ""} onClick={() => setSelected(report)}><span>{report.id}</span><span><b>{report.type}</b><small>{report.subject}</small></span><span><em className={`risk risk--${report.risk.toLowerCase()}`}>{report.risk}</em></span><span>{report.age}</span><span>{report.owner}</span></button>)}</section><aside className="case-panel">{selected ? <><div className="case-panel__head"><div><span>{selected.id}</span><h2>{selected.type}</h2></div><button type="button" onClick={() => setSelected(null)} aria-label="Close case"><X size={18} /></button></div><div className="case-risk"><AlertTriangle size={18} /><span><strong>{selected.risk} potential harm</strong>Review evidence before taking account action.</span></div><dl><div><dt>Reported item</dt><dd>{selected.subject}</dd></div><div><dt>Account</dt><dd>Verified professional · 3 years</dd></div><div><dt>Prior reports</dt><dd>1 resolved, no action</dd></div><div><dt>Evidence</dt><dd>Conversation excerpt, listing snapshot</dd></div></dl><div className="case-actions"><button type="button" className="button button--outline" onClick={() => resolve(selected.id)}>No violation</button><button type="button" className="button button--coral" onClick={() => resolve(selected.id)}>Restrict content</button></div><button type="button" className="escalate-button">Escalate for senior review</button></> : <div className="empty-state"><BadgeCheck size={28} /><h2>Case closed</h2><p>Select another report to continue review.</p></div>}</aside></div></div></main></div>; }
